// Phase 2+ — GameCanvas
// Skia canvas component. Hosts the game loop worklet and all draw calls via gameRenderer.
// Receives game state from GameScreen and renders entities, map, fog, weather.

export default function GameCanvas() {
  return null;
}
