// Phase 4a — LevelUpModal
// Level-up skill selection modal. Shows 3 random skill choices.
// Uses kit's pre-built Upgrade Preset.png (8 slots, weapon icons, skill icons, progress bars).
// Max 5 stacks per skill (except Specials: max 1).

export default function LevelUpModal() {
  return null;
}
