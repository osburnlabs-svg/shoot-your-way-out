// Phase 7 — Minimap
// 60x40px minimap using Minimap BG.png + 15 unit markers (5 types x 3 colors).
// Shows player (green), enemies (red), helicopter boss, bomber, pickups (yellow).

export default function Minimap() {
  return null;
}
