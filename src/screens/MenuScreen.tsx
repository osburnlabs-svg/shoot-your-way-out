// Phase 7 — MenuScreen
// Main menu: title, meta-stats panel, DEPLOY button, Settings button, Support the Dev button.
// Banner ad shown at bottom (hidden if support IAP purchased).
// Uses assets/ui/menu/ kit assets.

export default function MenuScreen() {
  return null;
}
