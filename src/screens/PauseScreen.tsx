// Phase 7 — PauseScreen
// Overlay pause menu: BACK (resume), MENU (quit to main), SETTINGS.
// Triggered by pause button in HUD corner.
// Uses assets/ui/pause/ kit assets (PAUSE PRESET.png).

export default function PauseScreen() {
  return null;
}
