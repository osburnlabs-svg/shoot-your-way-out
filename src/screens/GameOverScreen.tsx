// Phase 7 — GameOverScreen
// Shows hero death animation, run stats (score / time / level / kills).
// Revive prompt (rewarded ad) on first death. NEW HIGH SCORE badge if applicable.
// REDEPLOY and RETURN TO HQ buttons.
// Uses assets/ui/mission_failed/ kit assets.

export default function GameOverScreen() {
  return null;
}
